import 'package:flutter/material.dart';

class TvListScreen extends StatelessWidget {
  const TvListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('TV lists placeholder'));
  }
}